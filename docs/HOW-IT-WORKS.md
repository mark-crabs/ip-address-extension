# IP Address Fetching

The IP Address Extension fetched the user's public IP address from IPify API. The extension uses the `fetch()` method to retrienve the IP in a simple JSON format.

## API Used
>> `https://api.ipify.org/?format=json`
